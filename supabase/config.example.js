// Copy this file to supabase/config.js and replace the two placeholders.
// The publishable/anon key is safe for browser use only when RLS is configured.
window.THE_PACK_SUPABASE = {
  url: 'https://YOUR_PROJECT_REF.supabase.co',
  publishableKey: 'YOUR_PUBLISHABLE_OR_ANON_KEY'
};
